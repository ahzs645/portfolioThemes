# Portfolio Configuration
# Copy this file to .env and configure as needed

# ===========================================
# DISPLAY MODE
# ===========================================
# Set to 'gallery' to show the theme catalog/switcher
# Set to 'theme' to display a single theme (specify VITE_THEME_ID)
# Default: gallery
VITE_DISPLAY_MODE=gallery

# ===========================================
# THEME SELECTION (only used when VITE_DISPLAY_MODE=theme)
# ===========================================
# Available themes:
#   ansub-minimal, brutalist, chiang-v4, chiang-current, nick-computer,
#   brian-lovin, josh-bradley, hendo, pi, chizi, gerhard, alana-goyal,
#   amirmxt, felix-dorner, terminal, stammy, stefan-zweifel, dmythro,
#   dtc, kubre, paco
# Default: ansub-minimal
VITE_THEME_ID=ansub-minimal

# ===========================================
# SECTION EXCLUSIONS
# ===========================================
# Comma-separated list of sections to hide from the portfolio
# Available sections:
#   about, experience, projects, education, skills, languages,
#   awards, publications, presentations, volunteer, certifications,
#   professionalDevelopment, socialLinks
# Example: VITE_EXCLUDED_SECTIONS=volunteer,awards,publications
# Default: (none - all sections shown)
VITE_EXCLUDED_SECTIONS=

# ===========================================
# UI OPTIONS
# ===========================================
# Show/hide the top navigation bar (only in theme mode)
# Set to 'true' or 'false'
# Default: true
VITE_SHOW_TOP_BAR=true

# Default dark mode preference
# Set to 'true', 'false', or 'system' (uses system preference)
# Default: system
VITE_DEFAULT_DARK_MODE=system
